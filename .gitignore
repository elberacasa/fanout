node_modules/
dist/
coverage/
.DS_Store
*.log
.env
.env.*
!.env.example
.fanout/runs/
*.tsbuildinfo

# Screenshots the owner drops in while we work
Screenshot*.png

# Throwaway scripts for driving a real mission by hand. Never committed.
*.dev.mjs
*.dev.id
*.dev.log

# Copied into each package by scripts/prepack.mjs so every published tarball carries the licence.
packages/**/LICENSE
