node_modules/
dist/
coverage/
.DS_Store
*.log
.env
.env.*
!.env.example
.fanout/runs/
*.tsbuildinfo

# Screenshots the owner drops in while we work
Screenshot*.png

# Throwaway scripts for driving a real mission by hand. Never committed.
# A real mission on this repository writes these beside the driver script: records of one run.
*.dev.mjs
*.dev.id
*.dev.log
*.dev.diff
*.dev.json
mission.dev.*

# Copied into each package by scripts/prepack.mjs so every published tarball carries the licence.
packages/**/LICENSE

# The Codex CLI mirrors .claude/skills into here, rewritten for itself, whenever it runs in this repo.
# Its copy, not ours, and it regenerates: nothing here is authored.
.agents/
