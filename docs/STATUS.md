# Status

## Resume here (2026-09-11)

- **Where we are:** only the foundation exists. There is no code. Every document in `docs/` is the agreed design.
- **Phase:** P0 · The crew works. **Next:** milestone 1, Foundations (see `docs/ROADMAP.md`). Session 1 starts with
  `KICKOFF.md`.
- **Open decisions for session 1:**
  1. The final name. Check availability; see DECISIONS 0007.
  2. Confirm the stack (DECISIONS 0006).
  3. Verify which agent CLIs are installed and signed in on the owner's machine, and their headless flags (update
     ADAPTERS.md).
- **Owner's machine (known):** macOS, with Claude Code and the Codex CLI installed and signed in. Other CLIs are
  unknown.
- **Nothing is public yet.** Pushing to GitHub or publishing needs the owner's word.

## History

- 2026-09-11 · Foundation documents written (session 0).
